# 安全边界

- 密码使用哈希保存，Token 只保存摘要；Bearer 兼容路径继续保留。
- Cookie/CSRF、CORS、请求限流、输入长度限制、上传限制和请求 ID 日志继续生效。
- 系统模型 Key 只在服务端环境配置，日志会脱敏；浏览器、数据库和审计记录不接收用户个人模型 Key。
- 未配置系统模型时使用演示模式，不把演示结果当作真实 Provider 验证。
- SQLite、单进程限流和本地部署限制了多实例公网使用；公网、SaaS、计费和多租户不在当前范围。
